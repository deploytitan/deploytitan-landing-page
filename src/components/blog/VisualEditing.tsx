'use client'

/**
 * Placeholder for Sanity Visual Editing overlay.
 * Install @sanity/visual-editing and replace this implementation
 * once a SANITY_API_READ_TOKEN is configured.
 */
export function VisualEditing() {
  return null
}
